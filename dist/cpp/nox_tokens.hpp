// NOX Design System — C++
// Auto-generated by Style Dictionary. DO NOT EDIT.

#pragma once
#include <cstdint>
#include <string_view>

namespace nox_tokens {
  // Standard 1dp/1px border on all surfaces. Depth is defined by borders, never shadows.
  constexpr float BORDER_WIDTH_DEFAULT = 1f;
  // VOID uses 0 radius everywhere — brutalist, no rounding.
  constexpr float BORDER_RADIUS_NONE = 0f;
  constexpr float BORDER_RADIUS_SMALL = 0f;
  constexpr float BORDER_RADIUS_MEDIUM = 0f;
  constexpr float BORDER_RADIUS_LARGE = 0f;
  constexpr float BORDER_RADIUS_FULL = 0f;
  // Void does not use drop shadows. All elevation is communicated via border and background layering.
  constexpr float ELEVATION_NONE = 0f;
  // App background. Pure black — nav bar, status bar, root scaffold.
  constexpr uint32_t COLOR_BG_BASE = 0x000000;
  // Primary surface — cards, sheets, dialogs.
  constexpr uint32_t COLOR_BG_SURFACE = 0x0A0A0A;
  // Secondary surface — nested containers, input fields.
  constexpr uint32_t COLOR_BG_SURFACE2 = 0x161616;
  // Separator between list items.
  constexpr uint32_t COLOR_BG_DIVIDER = 0x111111;
  // 1dp border on GlassBox and all containers. Depth without shadow.
  constexpr uint32_t COLOR_BG_BORDER = 0x1A1A1A;
  // Full white — titles, active labels.
  constexpr uint32_t COLOR_TEXT_PRIMARY = 0xFFFFFF;
  // Muted — subtitles, metadata.
  constexpr uint32_t COLOR_TEXT_SECONDARY = 0xAAAAAA;
  // Tertiary / disabled state.
  constexpr uint32_t COLOR_TEXT_DISABLED = 0x808080;
  // Steel — default accent. User-selectable at runtime.
  constexpr uint32_t COLOR_ACCENT_DEFAULT = 0xA8B0BC;
  // Reserved for weather / climate extremes.
  constexpr uint32_t COLOR_ACCENT_BLUE = 0x6BA4C8;
  // Warm gold accent.
  constexpr uint32_t COLOR_ACCENT_YELLOW = 0xC4A96A;
  // Cool violet accent.
  constexpr uint32_t COLOR_ACCENT_PURPLE = 0x8B86B8;
  // Error and destructive actions.
  constexpr uint32_t COLOR_SEMANTIC_DANGER = 0xC87070;
  constexpr uint32_t COLOR_HABIT_BLUE_STEEL = 0x6B8CAE;
  constexpr uint32_t COLOR_HABIT_BLUE_SLATE = 0x5C7A8A;
  constexpr uint32_t COLOR_HABIT_BLUE_CYAN = 0x7EB8C9;
  constexpr uint32_t COLOR_HABIT_BLUE_TEAL = 0x6E9E9E;
  constexpr uint32_t COLOR_HABIT_BLUE_MIST = 0x829AAA;
  constexpr uint32_t COLOR_HABIT_BLUE_DARKICE = 0x7B9BA8;
  constexpr uint32_t COLOR_HABIT_GRAY_GRAPHITE = 0x9A9FA8;
  constexpr uint32_t COLOR_HABIT_GRAY_SILVER = 0xB8B4AE;
  constexpr uint32_t COLOR_HABIT_GRAY_SMOKE = 0x8C8C8C;
  constexpr uint32_t COLOR_HABIT_GRAY_GREENGRAY = 0xA0A89A;
  constexpr uint32_t COLOR_HABIT_GRAY_VIOLET = 0xA89FA8;
  constexpr uint32_t COLOR_HABIT_WARM_AMBER = 0xC4A96A;
  constexpr uint32_t COLOR_HABIT_WARM_EARTH = 0xA08C7A;
  constexpr uint32_t COLOR_HABIT_WARM_COPPER = 0xB8956A;
  constexpr uint32_t COLOR_HABIT_WARM_MUD = 0x9C8070;
  constexpr uint32_t COLOR_HABIT_WARM_SAND = 0xAA8860;
  constexpr uint32_t COLOR_HABIT_WARM_RUST = 0xA07858;
  constexpr uint32_t COLOR_HABIT_GREEN_SAGE = 0x7EAB8A;
  constexpr uint32_t COLOR_HABIT_GREEN_MOSS = 0x8FA68E;
  constexpr uint32_t COLOR_HABIT_GREEN_FOREST = 0x6E9078;
  constexpr uint32_t COLOR_HABIT_GREEN_LICHEN = 0x7A9480;
  constexpr uint32_t COLOR_HABIT_GREEN_FIR = 0x5E8068;
  constexpr uint32_t COLOR_HABIT_GREEN_FERN = 0x889E82;
  constexpr uint32_t COLOR_HABIT_PURPLE_LAVENDER = 0x8B86B8;
  constexpr uint32_t COLOR_HABIT_PURPLE_INDIGO = 0x7A7498;
  constexpr uint32_t COLOR_HABIT_PURPLE_AMETHYST = 0x9088A8;
  constexpr uint32_t COLOR_HABIT_PURPLE_PLUM = 0x806880;
  constexpr uint32_t COLOR_HABIT_PURPLE_MAUVE = 0x987890;
  constexpr uint32_t COLOR_HABIT_PURPLE_NIGHT = 0x6E6488;
  constexpr uint32_t COLOR_HABIT_RED_ROSE = 0xB87E7E;
  constexpr uint32_t COLOR_HABIT_RED_BRICK = 0xA87070;
  constexpr uint32_t COLOR_HABIT_RED_ROSEWOOD = 0xC49090;
  constexpr uint32_t COLOR_HABIT_RED_TERRACOTTA = 0x9E7878;
  constexpr uint32_t COLOR_HABIT_RED_SALMON = 0xB88880;
  constexpr uint32_t COLOR_HABIT_RED_ADOBE = 0xA87868;
  // Opacity of surface background inside GlassBox containers.
  constexpr float EFFECT_GLASS_SURFACEALPHA = 0.8f;
  // Opacity of grain/noise texture overlay on GlassBox.
  constexpr float EFFECT_GLASS_GRAINALPHA = 0.04f;
  // Opacity of the radial accent gradient emanating from top-left of the screen.
  constexpr float EFFECT_ACCENTGLOW_ALPHA = 0.05f;
  // Radius of the accent radial glow in dp/px.
  constexpr float EFFECT_ACCENTGLOW_RADIUS = 2000f;
  // 4dp / 4px
  constexpr float SPACING_XS = 4f;
  // 8dp / 8px
  constexpr float SPACING_SM = 8f;
  // 16dp / 16px — base unit
  constexpr float SPACING_MD = 16f;
  // 24dp / 24px
  constexpr float SPACING_LG = 24f;
  // 32dp / 32px
  constexpr float SPACING_XL = 32f;
  // 48dp / 48px
  constexpr float SPACING_2XL = 48f;
  // 64dp / 64px
  constexpr float SPACING_3XL = 64f;
  // Primary VOID display font. IBM Plex Sans — 7 weights, open-source, UI-optimized. https://fonts.google.com/specimen/IBM+Plex+Sans
  constexpr std::string_view FONT_FAMILY_DISPLAY = "IBM Plex Sans";
  // Condensed variant of IBM Plex Sans. Useful for dense data, labels, and space-constrained UI.
  constexpr std::string_view FONT_FAMILY_CONDENSED = "IBM Plex Sans Condensed";
  // Monospace — code blocks, terminal contexts, numeric data. Pairs naturally with IBM Plex Sans.
  constexpr std::string_view FONT_FAMILY_MONO = "IBM Plex Mono";
  constexpr float FONT_SIZE_XS = 10f;
  constexpr float FONT_SIZE_SM = 12f;
  constexpr float FONT_SIZE_BASE = 14f;
  constexpr float FONT_SIZE_MD = 16f;
  constexpr float FONT_SIZE_LG = 20f;
  constexpr float FONT_SIZE_XL = 24f;
  constexpr float FONT_SIZE_2XL = 32f;
  constexpr float FONT_SIZE_3XL = 48f;
  constexpr int FONT_WEIGHT_THIN = 100;
  constexpr int FONT_WEIGHT_EXTRALIGHT = 200;
  constexpr int FONT_WEIGHT_LIGHT = 300;
  constexpr int FONT_WEIGHT_REGULAR = 400;
  constexpr int FONT_WEIGHT_MEDIUM = 500;
  constexpr int FONT_WEIGHT_SEMIBOLD = 600;
  constexpr int FONT_WEIGHT_BOLD = 700;
  // em units. Tight — display headlines.
  constexpr float FONT_LETTERSPACING_TIGHT = -0.02f;
  constexpr float FONT_LETTERSPACING_NORMAL = 0f;
  // em units. All-caps labels, tags, badges.
  constexpr float FONT_LETTERSPACING_WIDE = 0.08f;
  // em units. Extreme caps, decorative.
  constexpr float FONT_LETTERSPACING_WIDER = 0.12f;
} // namespace nox_tokens
